# Build de producao do frontend (usado pela Vercel).
# Chamadas absolutas para a API na VPS, atras do Traefik com HTTPS.
#
# A origem da Vercel precisa estar em FRONTEND_ORIGIN no .env do backend
# (/root/datalens/.env), senao o browser bloqueia por CORS.
VITE_API_URL=https://api-datalens.r-fael.com.br
